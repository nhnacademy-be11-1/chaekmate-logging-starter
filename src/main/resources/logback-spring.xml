<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Spring Boot 기본 Logback 설정을 포함합니다. -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- prod profile: stdout + JSON 파일 로깅 추가 -->
    <!-- SERVICE_NAME 변수는 LoggingInitializer에 의해 동적으로 설정됩니다. -->
    <springProfile name="prod">
        <property name="LOG_HOME" value="/home/backend/be11-team1/logging/logs/${SERVICE_NAME:-unknown-service}"/>
        <property name="DOMAIN_NAME" value="${SERVICE_NAME:-unknown-service}-${server.port}"/>

        <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${DOMAIN_NAME}-${PID}.log</file>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"chaekmate-service":"${SERVICE_NAME:-unknown-service}"}</customFields>
                <mdc>
                    <includeMdcKeyName>traceId</includeMdcKeyName>
                    <includeMdcKeyName>spanId</includeMdcKeyName>
                    <includeMdcKeyName>userId</includeMdcKeyName>
                    <includeMdcKeyName>clientIp</includeMdcKeyName>
                    <includeMdcKeyName>requestMethod</includeMdcKeyName>
                    <includeMdcKeyName>requestUri</includeMdcKeyName>
                    <includeMdcKeyName>responseStatus</includeMdcKeyName>
                </mdc>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${DOMAIN_NAME}.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>30</maxHistory>
            </rollingPolicy>
        </appender>

        <!-- root에 JSON_FILE appender를 추가합니다. -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="JSON_FILE"/>
        </root>
    </springProfile>

</configuration>
